/**
 * Path stripping is a requirement for precedence matching.  If
 * there are two paths with the same match but the matching is not
 * complete then the resulting path should be stripped.
 *
 * Eg.
 * Full path:
 */
module.exports = function strip(fullPath, matchedPath, virtualPath) {

};
