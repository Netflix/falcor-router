/**
 * appends all the suffixes to the incoming paths then runs the
 * collapse algorithm followed by a router get.
 *
 * @param {PathSet} paths
 * @param {PathSet} suffix
 */
module.exports = function runSuffix(routerInstance, paths, suffixes) {

};

